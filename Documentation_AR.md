# التوثيق الشامل - مشروع لوحة القياس للموارد الذكية (Smart ERP Dashboard)

## 📌 مقدمة عن المشروع
مشروع **Smart ERP Dashboard** هو تطبيق ويب تفاعلي (WebAssembly) يهدف إلى تسليط الضوء على **تأثير التحول الرقمي وتطبيق التقنيات الحديثة على التنافسية المؤسسية**. يُعد هذا التطبيق نموذجاً بحثياً وأكاديمياً متكاملاً يقارن بين بيئة العمل بالطرق التقليدية (قبل التكنولوجيا) وبيئة العمل الحديثة (بعد التحول الرقمي).

يدعم النظام اللغتين العربية والإنجليزية بشكل كامل، ويتضمن واجهة مستخدم حديثة تعتمد على مبادئ (Glassmorphism) والألوان المؤسسية الداكنة.

---

## 🛠 التقنيات المستخدمة (Tech Stack)
* **إطار العمل الأساسي:** Blazor WebAssembly (.NET 8.0)
* **لغة البرمجة:** C# (C-Sharp)
* **واجهة المستخدم:** HTML5, CSS3 المتطورة (Scoped CSS)
* **الرسوم البيانية:** مكتبة Chart.js مدمجة عبر JSInterop
* **الأيقونات:** Bootstrap Icons

---

## 📂 شرح أقسام الموقع وصفحاته

### 1. صفحة النظرة العامة (`/` - Overview)
تعتبر لوحة القياس الرئيسية، وتهدف إلى إعطاء ملخص سريع للإدارة العليا.
* **زر التبديل (Toggle Switch):** يتيح للمستخدم التبديل فوراً بين حالتين: "الطرق التقليدية" (قبل التكنولوجيا) و"التحول الرقمي" (بعد التكنولوجيا).
* **مؤشرات الأداء الرئيسية (KPIs):** تتحدث ديناميكياً عند التبديل وتتضمن (نمو المبيعات، التكاليف التشغيلية، رضا العملاء، متوسط وقت الاستجابة).
* **الرسم البياني لنمو الإيرادات:** يعرض مقارنة تفاعلية بين نمو إيرادات المؤسسة مقابل منافسيها في السوق.

### 2. التقنيات الحديثة (`/tech-toolkit` - Tech Toolkit)
صفحة تستعرض الركائز الأربع التي يقوم عليها التحول الرقمي للمؤسسة:
* **البطاقات التفاعلية:** تعرض 4 مجالات رئيسية (الحوسبة السحابية، الذكاء الاصطناعي، أتمتة العمليات الروبوتية RPA، وأنظمة الدفع الذكية).
* **النوافذ المنبثقة (Modals):** عند النقر على أي تقنية، تظهر نافذة تشرح الأثر الاستراتيجي والميزة التنافسية التي تقدمها للمؤسسة بأرقام واقعية.

### 3. تجربة العملاء (`/customer-experience` - Customer Experience)
تركز على أثر الأتمتة على مستوى رضا العميل النهائي.
* **محاكي تتبع الطلبات (Order Tracker):** أداة بصرية تحاكي رحلة الطلب المؤتمتة بالكامل من لحظة الاستلام إلى التوصيل السريع.
* **روبوت الدعم الذكي (AI Chatbot):** محاكاة لخدمة العملاء الآلية التي تعمل على مدار الساعة طوال أيام الأسبوع، وتعرض أسئلة شائعة مع ردودها الفورية.

### 4. حاسبة العائد على الاستثمار (`/roi-calculator` - ROI Calculator)
أداة مالية تفاعلية مصممة لإقناع متخذي القرار بجدوى الاستثمار التقني.
* **المدخلات:** قطاع العمل، عدد الموظفين، الإيرادات السنوية، وحجم الإنفاق التقني الحالي.
* **النتائج الحية:** تقوم الخوارزمية بحساب التوفير السنوي المتوقع، زيادة الكفاءة، فترة استرداد رأس المال (Payback Period)، والعائد على مدى 3 سنوات.
* **معايير القطاع (Industry Benchmarks):** يمكن للمستخدم اختيار القطاع (صحة، تصنيع، لوجستيات، إلخ) لضبط نسب الكفاءة بناءً على المعايير العالمية.
* **رسم بياني مالي:** يعرض التوفير التراكمي مقابل الاستثمار المالي عبر السنوات.

### 5. مركز العمليات والكفاءة (`/operations-center` - Operations Center)
يستعرض كيف تقوم التكنولوجيا بتسريع العمليات الداخلية وخفض الهدر.
* **سير العمل (Workflows):** يحتوي على نماذج حية لـ (إدارة المخزون، خط الإنتاج، وضمان الجودة).
* **محاكاة المهام:** يعرض جدولاً زمنياً (Timeline) يقارن الوقت المستغرق لتنفيذ المهام يدوياً مقارنةً بالأتمتة، مع زر لتشغيل "محاكاة حية" تُظهر تقدم العملية.
* **ملخص التأثير:** لوحة تعرض عدد الساعات الموفرة أسبوعياً ونسب زيادة الكفاءة.

### 6. مركز التعاون للفريق (`/team-collaboration` - Team Collaboration Hub)
يسلط الضوء على تأثير بيئة العمل الرقمية على إنتاجية الموظفين.
* **أدوات التعاون:** قائمة بأدوات التواصل وإدارة المشاريع (قبل وبعد التطبيق) توضح الوقت الموفر.
* **مؤشرات الإنتاجية:** رسم بياني شريطي يقارن بين الإنتاجية في (العمل من المكتب، العمل عن بُعد، العمل الهجين).
* **إحصائيات الاتصال:** مقاييس سريعة توضح مدى تحسن التواصل بين الفرق متعددة التخصصات وخفض وقت إعداد الاجتماعات.

---

## ⚙️ هيكلية النظام والخدمات (Architecture & Services)

### 1. خدمة التوطين (LocalizationService)
* خدمة مخصصة (Singleton) لإدارة ثنائية اللغة (عربي / إنجليزي).
* تدعم التبديل المباشر من اليمين لليسار (RTL) للغة العربية ومن اليسار لليمين (LTR) للإنجليزية دون الحاجة لإعادة تحميل الصفحة (StateHasChanged).
* تحتوي على قاموس شامل يضم كافة النصوص الفنية والإدارية مترجمة باحترافية لتلائم المصطلحات الأكاديمية والتجارية.

### 2. مزود البيانات الوهمية (MockDataService)
* لفصل الواجهة عن البيانات، تم الاعتماد على واجهة `IMockDataService` لتزويد التطبيق بكافة الأرقام، الإحصائيات، والنصوص التفاعلية.
* يُسهل هذا الهيكل عملية ربط التطبيق لاحقاً بقاعدة بيانات حقيقية (API/Backend) دون الحاجة لتغيير كود الواجهات الأمامية (Razor Components).

### 3. ربط الجافا سكريبت (JS Interop)
* ملف `SmartERP.js` مخصص لإدارة مكتبة Chart.js، ويحتوي على دالتين أساسيتين: `initChart` لإنشاء الرسوم البيانية، و `updateChart` لتحديثها بشكل حي عند التبديل بين حالات التقنية أو اللغات.

---

## 🚀 كيفية التشغيل والتطوير
1. تأكد من تثبيت **.NET 8.0 SDK** (أو أحدث).
2. افتح المجلد الجذري للمشروع في محرر الأكواد (Visual Studio أو VS Code).
3. قم بتشغيل الأمر التالي في موجه الأوامر (Terminal) لاستعادة الحزم وبناء المشروع:
   ```bash
   dotnet build
   ```
4. لتشغيل التطبيق في المتصفح المحلّي:
   ```bash
   dotnet run
   ```
   أو
   ```bash
   dotnet watch run
   ```
5. سيفتح التطبيق تلقائياً على المنفذ المحلي (Localhost) لتتمكن من تصفح المشروع والتفاعل معه.
